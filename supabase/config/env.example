# Supabase Edge Function configuration for AMS
SUPABASE_URL="https://your-project.supabase.co"
SUPABASE_SERVICE_ROLE_KEY="env(SUPABASE_SERVICE_ROLE_KEY)"

# Zuplo gateway base URL (without trailing slash)
ZUPLO_BASE_URL="https://gateway.example.com"

# Letta configuration
LETTA_API_BASE_URL="https://api.letta.com"
LETTA_API_KEY="env(LETTA_API_KEY)"

# LiteLLM proxy is accessed via Zuplo, so no direct configuration needed here,
# but credentials should exist in the Zuplo project.
